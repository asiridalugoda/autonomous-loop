# Continue Task N after an interruption (usage limit or capacity error)

The previous turn on this thread stopped before finishing. Continue from the current state of the worktree <worktree>: inspect `git log --oneline <BASE>..HEAD` and `git status` first, keep every correct change already made, and complete the original request (<"the task brief" | "fix round R" | "the rebase job">) exactly as specified earlier on this thread. If a rebase is in progress, finish or abort it deliberately and say which.

Verify with the same commands the original request named. Commit locally (objective messages, no trailers). Append a section "Continuation after interruption" to <report file> listing what was already done, what this turn did, commands and output tails. Final message: the short status contract (Status, commits, one-line test summary, concerns, report path).
